// stateSet should be 'latest' if it's live or 'pbe' if it's the upcoming set
const stateSet = "latest";
export const currentSet = 8;
export const apiURL = `https://raw.communitydragon.org/${stateSet}/cdragon/tft/en_us.json`;